# EGCT 데이터 디렉토리

## 📁 파일 구조

### ✅ GitHub에 포함 (공개)
```
data/
├── README.md                    # 이 문서
├── holders.public.json          # 익명화된 보유자 정보
└── utxo_set.public.json         # 익명화된 UTXO 데이터
```

### 🔒 서버에만 존재 (비공개)
```
data/
├── holders.json                 # 실제 보유자 (이름, 이메일)
├── utxo_set.json               # 실제 UTXO
├── genesis_transaction.json     # Genesis 거래
├── registration_tokens.json     # 등록 토큰
├── registration_links.csv       # 등록 링크
└── personalized_emails/         # 개인화 이메일
```

## 🔄 자동 데이터 로딩

시스템은 `DataLoader.js`를 통해 자동으로 적절한 데이터를 선택합니다:

### 서버 환경 (운영)
1. `utxo_set.json` 존재 → **실제 데이터 사용**
2. ✅ 실제 거래 가능
3. ✅ 총 발행량 검증 정상
4. 🔒 개인정보 보호

### GitHub 환경 (개발/데모)
1. `utxo_set.json` 없음 → **공개 데이터 사용**
2. ⚠️ 데모 모드
3. ⚠️ 익명화된 데이터
4. ✅ 시스템 구조 학습 가능

## 💡 작동 원리
```javascript
// DataLoader가 자동 선택
const dataLoader = new DataLoader();
const data = await dataLoader.loadUTXOSet();

// 서버: 실제 데이터
// GitHub clone: 공개 데이터
```

콘솔 출력:
- 서버: `✓ 실제 UTXO 데이터 로드 (서버 운영 모드)`
- 데모: `📂 공개 UTXO 데이터 사용 (데모 모드)`

## 🔒 개인정보 보호

### GitHub에 절대 포함되지 않는 정보
- ❌ 실제 이름
- ❌ 실제 이메일 주소
- ❌ 주민등록번호
- ❌ 전화번호
- ❌ 주소

### GitHub에 포함되는 정보
- ✅ 시스템 구조
- ✅ 소스 코드
- ✅ 익명화된 예시 데이터
- ✅ 문서

## 📊 데이터 구조

### holders.public.json (예시)
```json
{
  "note": "개인정보가 제거된 공개 버전",
  "totalSupply": 100000000,
  "holders": [
    {
      "id": 0,
      "name": "보유자_A",
      "email": "holder_a@example.com",
      "balance": 30000
    }
  ]
}
```

### holders.json (실제 - 서버에만 존재)
```json
{
  "totalSupply": 100000000,
  "holders": [
    {
      "id": 0,
      "name": "실제이름",
      "email": "real@email.com",
      "balance": 30000
    }
  ]
}
```

## 🛠️ 운영 서버 설정

실제 데이터를 서버에 배포:
```bash
# 1. 서버에 실제 데이터 파일 존재 확인
ls -la /var/www/government-ai-systems/currency/data/

# 필수 파일:
# - holders.json ✓
# - utxo_set.json ✓
# - genesis_transaction.json ✓

# 2. 권한 확인
chown -R www-data:www-data data/
chmod 644 data/*.json

# 3. Git 상태 확인 (실제 데이터가 추적되지 않는지)
cd /var/www/government-ai-systems
git status data/

# 출력 예시:
# nothing to add (실제 데이터는 .gitignore에 의해 제외됨)
```

## 📝 라이선스

- **소스 코드**: CC BY-NC-SA 4.0
- **공개 데이터**: CC BY-NC-SA 4.0
- **실제 개인정보**: 비공개 (개인정보보호법)

## ⚠️ 주의사항

1. **절대 커밋하지 말 것**
   - `holders.json`
   - `utxo_set.json`
   - `registration_*.json`

2. **커밋 전 확인**
```bash
   git status | grep "data/"
   # 개인정보 파일이 표시되면 안 됨!
```

3. **실수로 커밋한 경우**
   - 즉시 repository 히스토리에서 제거
   - 또는 새 repository로 이전

## 🎯 목표 달성

✅ GitHub에 개인정보 없음
✅ 서버에서 정상 작동
✅ 총 발행량 검증 가능
✅ 개발자가 데모 가능
✅ 개인정보보호법 준수
